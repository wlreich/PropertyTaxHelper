# PAR-20 neighborhood analysis and print view

Base: `caf18d1698ab89355700cd1e8983bbd0622e5381` (merged PAR-19).

## Delivered

The neighborhood screen and browser-print route consume `getNeighborhoodAnalysis(id, activeSeason(calendar))`. Both use `NeighborhoodAnalysisView`, so current periods, completed outcomes, historical lenses, caps, multiplier inputs, coverage and explanations have one rendering contract. The global snapshot selector is removed. Existing overview/compare/guide navigation and the canonical `#exemptions-heading` cap destination are preserved.

The approved Figma frame `EdxFjIcxiEfKxM6gbwoW7o / 28:11` was opened through design context, including its screenshot and sections 28:18–28:22. Desktop hierarchy matches the current-story panel, distribution/outcomes pair, compact multiplier, two historical lenses, cap strip and actions. Existing shared header/navigation, accessible brand colors and additional denominator/inference explanations intentionally take precedence over abbreviated mockup content. There is no approved mobile Figma frame; mobile stacks in reading order.

No valuation formula, cohort, database permission, source-selection rule or migration changed. The cap strip calls the existing `summarizeGroup` against eligible current preliminary observations. Completed outcomes use `latestOutcome`; current-summary legacy comparisons are not substituted. Paired/conditional denominators and missing counts remain explicit. Samples below ten lead with counts. The 10% carry-forward calculation comes unchanged from PAR-19.

The print view uses the existing browser-print mechanism. It includes both distribution modes so either screen selection is represented, plus expanded outcome/method details. Pagination flows with content instead of truncating into the previous fixed two-sheet layout. Navigation, support actions and the print toolbar are suppressed in print media. The unrelated guide PDF is unchanged.

## Finite verification

- `npm ci`; `npm run verify`: 93 unit tests, lint, build, type checks, formatting, brand and repository-required content checks passed.
- Existing `render-smoke.mjs`: passed, with neighborhood assertions updated for the approved content and shared print layout.
- Existing neighborhood Playwright suite: one complete 1440px journey for section order, actual SQL-backed data, toggles, keyboard focus/activation, disclosure state, canonical overview/cap/compare/guide/support destinations, print invocation and text parity; axe WCAG checks passed.
- Compact deterministic states: preliminary 2026 with completed 2025 outcomes; an additional 2024→2025 pair; three-home count-first patterns; zero qualifying reductions; missing periods and values. No fabricated zero price, NaN or infinity.
- Responsive coverage: screenshots at 390 and 1440px, overflow checks at 320 and 768px, and 200% zoom. The fixture journey does not repeat at every width. Four explicit skips reflect this intended scope.
- The existing market-adjustment browser test is updated for the compact disclosure; property-overview checks and schedule download checks remain intact.
- Browser evidence is generated under `web/brand-test-results` and retained by the existing `parcelsavvy-brand-review` CI artifact. No competing QA workflow was created.

Local Playwright's browser download timed out; local checks used the already-installed Chromium 153 through a temporary, uncommitted launch configuration. CI continues to use its normal pinned Playwright Chromium. The temporary configuration is not part of the change.

## Review and release

Implementation review focused on phase/period labeling, available matched cohorts, null/zero handling, cap denominators, print parity, accessible controls and preservation of existing links. Initial shared CSS specificity and definition-list accessibility findings were fixed. Print section-break improvements were checked on the changed report only.

Brand: approved logo and shared navigation retained; Manrope/Inter, semantic colors/spacing, tabular numerals and visible control focus used. Restrained green chart bars follow the approved visual direction using the existing semantic palette; functional labels/markers use Action Blue. No school-district navigation, historical import, guide redesign, public raw data or new PDF service.

Final candidate, required CI, live-preview smoke, screenshot/PDF inspection and merge state are recorded in the PR and Linear completion comment. PAR-19's reconciliation evidence is reused rather than repeated.
