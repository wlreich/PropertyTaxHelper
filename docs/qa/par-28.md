# PAR-28 property report evidence

September 21, 2026. Standalone `/property/[id]/print` uses the overview's selected release and existing assessment, outcome, cap, history and neighborhood models. Missing and withheld values remain unavailable. No database schema, valuation formula or public access rule changes.

Reviewed current Figma summary, inventory, valuation, neighborhood, history, tax/review and long-content references in file EdxFjIcxiEfKxM6gbwoW7o. The report adapts their hierarchy to complete source records and browser pagination; it does not limit history or inventory to a fixed number of pages.

The required actual-property Letter reference was rendered and reviewed privately (7 pages, 11 inventory rows, 2 history years, five taxing authorities). Its property capture and PDF are **not committed or uploaded to public CI**. Automated approval review rejected publishing that reference to the public repository; the committed regression fixture was therefore independently rebuilt with entirely synthetic addresses, identifiers, values, features and authority records. No actual-property association is carried into the synthetic fixture. Neighborhood cohorts in all committed cases are synthetic too.

The bounded public browser suite produces a populated synthetic report, 26-feature/three-improvement inventory case, 26-year history with a missing year and excluded baseline, sparse preliminary record, and one dense A4 grayscale fallback with background graphics disabled. It checks every feature/year, explicit continuation of pathological labels, missing-versus-zero, source coverage, subject/year/page totals, absence of application navigation, and heading/content continuity. Rasterized documents were reviewed for numeric alignment, full records, readable type and pagination. Final public synthetic counts: 7 / 9 / 9 / 1 Letter pages; 8 A4 pages. CI artifact links are recorded in the PR.

The narrow keyboard journey checks entry, retained release, font readiness, print invocation, Back, invalid release, axe and overflow. Three focused model tests reuse existing arithmetic and privacy coverage. Full `npm run verify` passed 113 unit tests plus lint/build/type/brand checks; mandatory CI rechecks the final synthetic fixture and existing responsive/search/access gates.

Production uses the existing live RPC and requires property/release agreement before displaying neighborhood aggregates. Condition/effective age and lot-area medians are not supplied by the current public contract; the report says so. No private Figma imagery, owner/contact fields, actual-property capture or private appeal data are published by this change.

Brand: approved BrandLogo, Manrope/Inter, semantic tokens, Appraisal District wording and evidence-led qualifications. Letter uses 0.5-inch margins, 10.5-point body and 9-point supporting text. Small records use a compact layout without shrinking required text. The PR/Linear completion record reports actual deployment; this evidence map does not assert deployment on its own.

Review follow-up: protest observations are bounded to the selected year and export date; later observations cannot rewrite historical reports. Search query, result page and all-results context survive print and Return. Four report model tests, lint/build and the narrow keyboard return-path journey passed.
