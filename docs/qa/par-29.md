# PAR-29 neighborhood report evidence

September 21, 2026. The standalone neighborhood report now leads with current same-period subject/group values and actual eligible denominators, followed by a compact distribution, outcomes, cross-year proposal/carry-forward patterns, isolated multiplier context and selected activity.

The existing neighborhood calculations and PAR-32 evidence-window/selection contract remain the source of truth. Target year, research dates, filter, sort and selected records survive print and Back. Records outside the active type filter remain in the chosen shortlist. Missing records/coverage remain explicit. No transaction-price inference, new feed, database change or access-rule change.

Reviewed Figma report references 91:151 and 93:151 in EdxFjIcxiEfKxM6gbwoW7o. Two pages is a density target, not a truncation limit. The standard populated synthetic Letter report is 2 pages. The long-address, 24-record synthetic shortlist is 5 Letter pages. Its single A4 grayscale fallback is 4 pages. All were rasterized and reviewed for readable type, aligned amounts, intact rows, repeated headers, page identity, source qualifications and no empty/trailing-furniture pages. Every selected property appears exactly once in extracted PDF text. Fixed fixture page counts are regression assertions.

Focused local verification: `npm run verify` passed 110 unit tests plus lint/build/type/brand checks; affected model/date/export coverage is reused. Final browser suite (`neighborhood`, `activity`, `neighborhood-report`) passed 11 tests, with 10 intentional skips across viewport projects. It includes one narrow keyboard/axe/print-and-Back journey, retained selected records outside the filter, existing PAR-32 CSV/state parity, default/current labels, no-data/unavailable prior-year coverage, responsive behavior and existing small-sample checks. Final lint passed. Required CI remains the merge gate, and its browser artifacts retain representative synthetic PDFs/screenshots.

Brand: approved BrandLogo, Manrope/Inter, semantic tokens and Appraisal District wording. Letter uses 0.5-inch margins and 10.5pt body text; supporting notes use 9pt. Sources distinguish appraisal releases, transaction dates and export coverage. Neighborhood groups are not matched comparables; reductions do not establish protest causation or tax savings. Long shortlists deliberately continue without truncation. Browser-generated PDF pagination depends on the paper/printer settings; the tested Letter/A4 settings are explicit.

Review follow-up: small matched cohorts lead with their actual home count, with percentage in supporting text, reusing MIN_PATTERN_SAMPLE. Each combined chart bin has an accessible range and count. Targeted small-cohort browser semantics, lint and build passed.
