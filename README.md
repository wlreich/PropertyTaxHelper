# ClearValue Property Search

A responsive property-tax search landing page backed by local mock records.

## Run locally

```bash
npm install
npm run dev
```

Use `npm test` to run the component and search tests, or `npm run build` to
create a production build.
